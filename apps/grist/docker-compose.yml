services:
  grist:
    image: gristlabs/grist:latest
    container_name: grist
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "6110:8484"
    volumes:
      - grist_data:/persist
volumes:
  grist_data:
    external: true
